
const toDoList = [];
const listaProdutos = document.getElementById('lista_produtos');
const addItem = document.getElementsByClassName('adicionar_carrinho')[0]

        
        function newItem(){
            const carrinho = document.querySelector('.meu_carrinho')
            

            const listItem = document.createElement('li');

            const itemFoto = document.getElementsByClassName('foto_produto')[0];
            const itemName = document.getElementsByClassName('nome_produto')[0];
            const itemPreco = document.getElementsByClassName('preco')[0];
            const itemRemove = document.createElement('button');
            itemRemove.innerText = 'Remover Produto';

            itemFoto.


            
            
            listItem.appendChild(itemFoto);
            listItem.appendChild(itemName);
            listItem.appendChild(itemPreco);
            listItem.appendChild(itemRemove);
            carrinho.appendChild(listItem);
            
            toDoList.push(listItem);
        }

addItem.addEventListener('click', newItem);

























































































































































































































































































































































/*
const elementCarro = document.getElementsByClassName('meu_carrinho')[0]

const itemList = document.createElement('li')

itemList.innerText = 'Novo produto'

elementCarro.appendChild(itemList)
*/

